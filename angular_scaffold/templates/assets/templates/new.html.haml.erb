%h4
  = { "translate" => 'creation_page' } 

%div{"ng-controller" => "<%= class_name.camelize(:upper).pluralize %>EditController"}
  = render_haml("<%= class_name.tableize %>/_form.html.haml")
  .form-group
    %form{"ng-submit" => "addPhone()"}
