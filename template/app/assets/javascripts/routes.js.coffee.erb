angular.module('<%= @AppName %>').config ($stateProvider, $urlRouterProvider, Rails) ->
  $urlRouterProvider.otherwise('/');

  $stateProvider.state('home', {
    url: '/',
    controller: 'PagesCtrl',
    templateUrl: 'pages/index.html'
  })
