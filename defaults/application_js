/**
 * Defining routes
 */
app_name.config(['$routeProvider',
  function($routeProvider){
    $routeProvider.
      otherwise({
        redirectTo: '/'
      });
  }
]);

/**
 * Controlleur des messages flash
 */
function FlashCtrl($scope){}